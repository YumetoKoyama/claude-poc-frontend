# フロントエンド技術スタック確定表

> 本ファイルはフロントエンド技術スタックの**正典**であり、設計着手前ハードゲートの機械チェック対象である。
> 「状態」列に `要確定` が 1 行でも残る間は、`design-from-requirements` 以降に進んではならない
> （`.claude/skills/_common/scripts/check-stack-decided.sh` が検査する）。
> 確定の記入は**人間が行う**。Claude は既定値で補完しない（候補の提案までは可）。
> 技術スタックの矛盾時は frontend ルールを最優先（正）とする（CLAUDE.md）。

## 凡例

| 略号・値 | 正式名称（日本語） | 補足 |
|---|---|---|
| 確定 | 人間が確定済み | 確定値・確定日を記入済みであること |
| 要確定 | 未確定（設計着手ブロック） | 人間が確定値を記入し `確定` に書き換えるまで設計に進まない |

## 確定表

| # | 項目 | 確定値 | 状態 | 確定日 | 根拠・備考 |
|---|------|--------|------|--------|------------|
| 1 | 言語 | TypeScript | 確定 | 2026-06-04 | `frontend-state-api.md` |
| 2 | フレームワーク | Next.js | 確定 | 2026-06-04 | `frontend-directory-structure.md`（`next.config.ts`） |
| 3 | フレームワークバージョン | 16.2.7 + App Router | 確定 | 2026-06-08 | App Router は `frontend-routing.md` で確定済み。16.2 系 LTS |
| 4 | Node.js バージョン | 24.16.0 | 確定 | 2026-06-08 | LTS |
| 5 | パッケージマネージャ | npm | 確定 | 2026-06-08 | CI allowlist（`npm`/`npx`）と整合 |
| 6 | 状態管理（UI state） | Zustand | 確定 | 2026-06-04 | `frontend-state-api.md` |
| 7 | サーバーデータキャッシュ | なし（素の fetch で運用） | 確定 | 2026-06-08 | デモ段階のためキャッシュ層を導入しない。一覧/詳細が増え再取得制御が必要になった場合は React Query 等の導入を再検討（後付けは広範囲改修になる点に留意） |
| 8 | 単体テスト | Vitest + React Testing Library | 確定 | 2026-06-04 | `frontend-testing-naming.md` |
| 9 | リンタ・フォーマッタ | ESLint + Prettier | 確定 | 2026-06-08 | |
| 10 | カバレッジ閾値 | 100%（**除外後**） | 確定 | 2026-06-08 | **除外後 100% で運用する。計測対象から自動生成型・style ファイル・フレームワーク接着層を除外し、各除外に理由（`/* istanbul ignore next */` 等）を必須とする。除外なしの素の 100% は緑化目的のノイズテストを誘発するため採らない** |
| 11 | UI コンポーネント・スタイリング | Tailwind CSS | 確定 | 2026-06-08 | |

## 運用ルール

- 行の追加は可（項目の細分化など）。行の削除・「状態」列の廃止は不可（機械チェックの前提が壊れる）。
- 確定時は「確定値」「確定日」を記入し、「状態」を `確定` に書き換える。判断の経緯が長い場合は `docs/process/` 側に記録し備考から参照する。
- 本ファイルは `.claude/rules/` 配下のため protect-canon の保護対象。編集は人手（または `ALLOW_RULES_EDIT=1` セッション）で行う。
